package co.edu.eafit.carritocompras.service;

//import java.util.List;
//import java.math.BigDecimal;

public class loyaltyPoints {
 
	public int pointsCalculate() {
		/*
		int points;
		int pointHelp=1000;
		BigDecimal pointHelp = new BigDecimal(pointHelp);
		BigDecimal totalPoints = new BigDecimal(0);

		totalPoints = pointHelp.divide(totalPrice);

		points = totalPoints.intValueExact();
		setPoints(points);
		return points;
	*/
	int points=200;
	return points;
	}
	
	
}
